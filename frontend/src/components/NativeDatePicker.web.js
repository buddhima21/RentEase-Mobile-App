import React from 'react';

export default function NativeDatePicker(props) {
  // Web uses standard HTML input, so this component returns null 
  // or a fallback if not handled by the parent
  return null;
}
